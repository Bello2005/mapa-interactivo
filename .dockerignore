# Historial de git: ~98 MB que no aportan nada al build.
.git
.gitignore
.gitattributes

# Dependencias y artefactos: se reconstruyen dentro de la imagen.
node_modules
dist
dist-ssr
*.local

# Lockfile alterno: la imagen usa npm ci con package-lock.json.
pnpm-lock.yaml

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editores y sistema
.vscode
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Documentación y material que no entra al bundle.
README.md
for_IIAP
vercel.json

# Docker
Dockerfile
.dockerignore
docker-compose.yml
